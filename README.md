## TODO DO

### Adicionar uma sanitização XSS nos campos, usando a lib validator (npm install validator)
### Adicionar uma camada de segurança maior nas querys, usando DTO (Data Transfer Object) + Serialização (Camada da Aplicação), usando zod, para parsear os dados retornados 